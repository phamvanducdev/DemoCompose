signingConfigs {
  debug {
    storeFile file('keys/debug.jks')
    storePassword 'Duc@260602'
    keyAlias 'debug'
    keyPassword 'Duc@260602'
  }
  release {
    storeFile file('keys/release.jks')
    storePassword 'Duc@260602'
    keyAlias 'release'
    keyPassword 'Duc@260602'
  }
}
